[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @freelancer/web build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.30.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
